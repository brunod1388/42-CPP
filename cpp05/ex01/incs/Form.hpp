#ifndef FORM_HPP
# define FORM_HPP

#include <string>

class Form {

private:

protected:

public:

	Form(void);
	Form(const Form &src);
	~Form(void);

	Form & operator=(const Form &rhs);

};

#endif
